import { Text, View } from 'react-native';

const LotteriesScreen = () => {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text>LotteriesScreen</Text>
    </View>
  );
};

export default LotteriesScreen;
